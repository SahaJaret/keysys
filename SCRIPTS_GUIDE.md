# 📜 Scripts Manager - Руководство

## ✨ Возможности

### 1. **Управление скриптами**
- ✅ Создание и редактирование Lua скриптов
- ✅ Загрузка через удобный веб-интерфейс
- ✅ Система версионирования (автоматически)
- ✅ Включение/отключение скриптов
- ✅ Поиск по названию, slug или описанию

### 2. **Доставка скриптов для Roblox**
- ✅ Уникальные URL для каждого скрипта
- ✅ Готовый код для `loadstring()`
- ✅ Поддержка UTF-8 (русский язык и эмодзи)
- ✅ Быстрая доставка через HTTP

### 3. **Обфускация**
- ✅ Базовая защита кода
- ✅ Удаление комментариев
- ✅ Минимизация кода
- ✅ Обфускация переменных

### 4. **Аналитика**
- ✅ Счетчик загрузок
- ✅ Дата последнего доступа
- ✅ Размер скрипта
- ✅ История версий

## 🚀 Как использовать

### Создание нового скрипта

1. **Откройте админ панель**
   ```
   http://your-domain.com/admin?pass=admin123&page=scripts
   ```

2. **Нажмите "New Script"**

3. **Заполните поля:**
   - **Script Name**: Имя вашего скрипта (например, "ESP Hack")
   - **Slug**: URL идентификатор (автоматически генерируется, например, "esp-hack")
   - **Description**: Краткое описание (опционально)
   - **Lua Code**: Ваш Lua код
   - **Enable Script**: Включить сразу после создания
   - **Obfuscate Code**: Применить обфускацию

4. **Сохраните**

### Получение ссылки на скрипт

После создания вы увидите:

**Loadstring URL:**
```
http://your-domain.com/script/esp-hack
```

**Roblox Code:**
```lua
loadstring(game:HttpGet("http://your-domain.com/script/esp-hack"))()
```

### Использование в Roblox

Игрок просто копирует и вставляет код в executor:

```lua
loadstring(game:HttpGet("http://your-domain.com/script/esp-hack"))()
```

## 📝 Примеры скриптов

### Пример 1: Простой скрипт

**Название:** Hello World  
**Slug:** hello-world

```lua
-- Simple greeting script
local player = game.Players.LocalPlayer
print("Hello, " .. player.Name .. "!")

-- Create a notification
game.StarterGui:SetCore("SendNotification", {
    Title = "Welcome!";
    Text = "Script loaded successfully";
    Duration = 5;
})
```

### Пример 2: Fly Script

**Название:** Fly Hack  
**Slug:** fly-hack

```lua
-- Fly script for Roblox
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")

local flying = false
local speed = 50

-- Toggle fly on/off
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.F then
        flying = not flying
        if flying then
            print("Flying enabled")
        else
            print("Flying disabled")
        end
    end
end)

-- Fly loop
game:GetService("RunService").Heartbeat:Connect(function()
    if flying and character and character:FindFirstChild("HumanoidRootPart") then
        local hrp = character.HumanoidRootPart
        local bodyVelocity = Instance.new("BodyVelocity")
        bodyVelocity.Velocity = Vector3.new(0, 0, 0)
        bodyVelocity.MaxForce = Vector3.new(4000, 4000, 4000)
        bodyVelocity.Parent = hrp
        
        wait(0.1)
        bodyVelocity:Destroy()
    end
end)
```

### Пример 3: Anti-AFK

**Название:** Anti AFK  
**Slug:** anti-afk

```lua
-- Anti-AFK script
local player = game.Players.LocalPlayer

game:GetService("RunService").Stepped:Connect(function()
    -- Prevent AFK kick
    for _, connection in pairs(getconnections(player.Idled)) do
        connection:Disable()
    end
end)

print("Anti-AFK enabled - you won't be kicked!")
```

## 🔒 Обфускация

### Что делает обфускация?

1. **Удаляет комментарии** - Убирает все `--` комментарии
2. **Минимизирует код** - Удаляет лишние пробелы
3. **Оборачивает в функцию** - Добавляет защитный wrapper

### Пример

**До обфускации:**
```lua
-- My secret script
local player = game.Players.LocalPlayer
print("Hello " .. player.Name)
```

**После обфускации:**
```lua
local _0xabc123 = (function()
  local player = game.Players.LocalPlayer print("Hello " .. player.Name)
end)()
return _0xabc123
```

### ⚠️ Важно!
Базовая обфускация НЕ защищает на 100%! Для серьезной защиты используйте:
- [Luraph](https://lura.ph/) (платно)
- [IronBrew](https://github.com/Sxpt/Ironbrew) (бесплатно)
- [PSU](https://luau.io/) (платно)

Вы можете обфусцировать код внешним инструментом и загрузить уже защищенный скрипт.

## 📊 Версионирование

### Автоматическое

При каждом редактировании скрипта с изменением кода:
- Создается новая версия
- Старые версии сохраняются
- URL остается прежним (автоматически отдается последняя версия)

### Структура версии

```javascript
{
  version: 1,
  content: "обфусцированный код",
  rawContent: "оригинальный код",
  createdAt: "2024-01-01T00:00:00.000Z",
  createdBy: "admin"
}
```

### История версий

В будущем можно добавить:
- Просмотр всех версий
- Откат к предыдущей версии
- Сравнение версий

## 🔗 API Endpoints

### Публичный доступ

**GET /script/:slug**
- Возвращает Lua код скрипта
- Только для enabled скриптов
- Обновляет статистику

**Пример:**
```bash
curl http://localhost:3000/script/hello-world
```

### Админ эндпоинты (требуют авторизации)

**GET /admin/scripts/get?id=:id&pass=:pass**
- Получить скрипт по ID для редактирования

**POST /admin/scripts/add**
```json
{
  "pass": "admin123",
  "name": "My Script",
  "slug": "my-script",
  "description": "Description here",
  "content": "print('Hello')",
  "enabled": true,
  "obfuscated": false
}
```

**POST /admin/scripts/edit**
```json
{
  "pass": "admin123",
  "id": "script_id",
  "name": "Updated Name",
  "slug": "updated-slug",
  "description": "Updated description",
  "content": "print('Updated code')",
  "enabled": true,
  "obfuscated": true
}
```

**POST /admin/scripts/delete**
```json
{
  "pass": "admin123",
  "id": "script_id"
}
```

## 📈 Аналитика

### Что отслеживается

- **totalDownloads**: Сколько раз скрипт был загружен
- **lastAccessed**: Когда последний раз к скрипту обращались

### Просмотр статистики

В админ панели для каждого скрипта показывается:
- Количество загрузок
- Текущая версия
- Размер в KB
- Дата последнего доступа

## 🎯 Лучшие практики

### 1. **Naming**
- Используйте понятные имена
- Slug должен быть коротким и запоминающимся
- Избегайте спецсимволов в slug

### 2. **Безопасность**
- Не храните пароли или токены в скриптах
- Используйте обфускацию для важных скриптов
- Регулярно обновляйте скрипты

### 3. **Производительность**
- Минимизируйте размер скриптов
- Используйте локальные переменные
- Избегайте бесконечных циклов

### 4. **Тестирование**
- Всегда тестируйте скрипты перед публикацией
- Проверяйте на разных играх
- Отслеживайте ошибки

## 🔧 Troubleshooting

### Скрипт не загружается

1. Проверьте что скрипт enabled
2. Проверьте правильность slug
3. Убедитесь что сервер запущен

### Ошибка в коде

1. Проверьте синтаксис Lua
2. Используйте `print()` для отладки
3. Проверьте логи в Output (F9 в Roblox)

### Обфускация не работает

1. Проверьте что чекбокс "Obfuscate" включен
2. При редактировании нужно пересохранить
3. Базовая обфускация может не сработать на сложных скриптах

## 🚀 Что дальше?

### Планируемые функции

- [ ] Drag & drop загрузка файлов
- [ ] Syntax highlighting в редакторе
- [ ] Продвинутая обфускация (интеграция с внешними сервисами)
- [ ] Папки и категории
- [ ] Шаблоны скриптов
- [ ] Экспорт/импорт скриптов
- [ ] Комментарии и рейтинги
- [ ] API ключи для доступа
- [ ] Rate limiting
- [ ] CDN интеграция

---

**Готово! Теперь у вас полноценная система управления скриптами для Roblox! 🎉**

Все скрипты доступны по простым ссылкам и готовы к использованию в loadstring().

