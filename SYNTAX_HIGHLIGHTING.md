# 🎨 Подсветка синтаксиса Lua

## ✅ Что реализовано:

### 1. **Оригинальный код при редактировании**
Теперь при редактировании скрипта показывается **оригинальный** код, а не обфусцированный!

**До:**
```lua
-- Обфусцированный код
local _anjdqbfu={20,1,6,22,3,76...}
```

**После:**
```lua
-- Оригинальный код
local player = game.Players.LocalPlayer
print('Hello ' .. player.Name)
```

### 2. **Подсветка синтаксиса Lua в реальном времени**
Красивая подсветка синтаксиса прямо в редакторе! 🎨

#### Что подсвечивается:

| Элемент | Цвет | Пример |
|---------|------|--------|
| **Ключевые слова** | 🟣 Фиолетовый | `local`, `function`, `if`, `end`, `for`, `while` |
| **Встроенные функции** | 🔵 Синий | `print`, `game`, `workspace`, `wait`, `spawn` |
| **Типы данных** | 🩷 Розовый | `string`, `number`, `boolean`, `table` |
| **Строки** | 🟢 Зеленый | `"Hello World"`, `'текст'` |
| **Числа** | 🟠 Оранжевый | `123`, `45.67`, `0.5` |
| **Комментарии** | ⚫ Серый | `-- комментарий`, `--[[ блок ]]` |

### 3. **Интерактивный редактор**
- ✅ Подсветка обновляется в реальном времени при вводе
- ✅ Синхронизированный скролл
- ✅ Счетчик символов
- ✅ Моноширинный шрифт
- ✅ Tab = 2 пробела
- ✅ Проверка орфографии отключена

---

## 🎯 Как это выглядит:

### Пример кода с подсветкой:

```lua
-- ESP Script for Roblox (комментарий - серый)
local player = game.Players.LocalPlayer  -- local, game - цветные
local camera = workspace.CurrentCamera

-- Configuration
local settings = {
    enabled = true,               -- true - фиолетовый
    boxColor = Color3.fromRGB(255, 0, 0),  -- числа - оранжевые
    nameColor = Color3.fromRGB(255, 255, 255)
}

print('ESP Loaded successfully!')  -- print - синий, строка - зеленая

function updateESP()  -- function - фиолетовый
    for i, v in pairs(game.Players:GetPlayers()) do  -- for, in, pairs - цветные
        if v ~= player then  -- if - фиолетовый
            -- Код ESP
        end  -- end - фиолетовый
    end
end
```

### В редакторе это выглядит так:
- 🟣 `local` и `function` выделены фиолетовым
- 🔵 `game`, `workspace`, `print`, `pairs` выделены синим
- 🟢 Все строки в кавычках зеленые
- 🟠 Числа `255`, `0` оранжевые
- ⚫ Комментарии серые и курсивом

---

## 📝 Использование:

### 1. Создание нового скрипта:
1. Откройте админ панель → Scripts
2. Нажмите "New Script"
3. Начните печатать код
4. **Подсветка появляется автоматически!** ✨

### 2. Редактирование существующего скрипта:
1. Нажмите кнопку "Edit" на скрипте
2. **Видите оригинальный код** (не обфусцированный!)
3. Редактируйте с подсветкой синтаксиса
4. Сохраните

### 3. Пример:

**Вы создали обфусцированный скрипт:**
```lua
-- Сохранено с обфускацией
local _abc={27,3,16...}; -- Зашифровано
```

**При редактировании видите:**
```lua
-- Оригинальный код
local player = game.Players.LocalPlayer
print('Hello!')
```

---

## 🔧 Технические детали:

### Реализация:
```javascript
// Подсветка синтаксиса
function highlightLua(code) {
  // 1. Escape HTML
  code = code.replace(/&/g, '&amp;')...
  
  // 2. Подсветка комментариев
  code = code.replace(/(--\[\[[\s\S]*?\]\]|--[^\n]*)/g, 
    '<span style="color:#6b7280">$1</span>');
  
  // 3. Подсветка строк
  code = code.replace(/(".*?"|'.*?')/g, 
    '<span style="color:#10b981">$1</span>');
  
  // 4. Подсветка ключевых слов
  code = code.replace(/\b(local|function|if|end|for|while)\b/g, 
    '<span style="color:#8b5cf6">$1</span>');
  
  // 5. И т.д.
  return code;
}
```

### Хранение данных:
```javascript
{
  versions: [
    {
      version: 1,
      content: "-- Обфусцированный код...",  // Для выдачи пользователям
      rawContent: "-- Оригинальный код...",   // Для редактирования
      createdAt: Date,
      createdBy: "admin"
    }
  ]
}
```

---

## 🎨 Настройка цветов:

### Текущая цветовая схема:

```javascript
// Ключевые слова
color: #8b5cf6  // Фиолетовый (purple-500)

// Встроенные функции
color: #3b82f6  // Синий (blue-500)

// Типы данных
color: #ec4899  // Розовый (pink-500)

// Строки
color: #10b981  // Зеленый (emerald-500)

// Числа
color: #f59e0b  // Оранжевый (amber-500)

// Комментарии
color: #6b7280  // Серый (gray-500)
```

### Можно изменить:
Отредактируйте функцию `highlightLua()` в `server (2).js` строка ~2138

---

## 🚀 Преимущества:

### Для разработчика:
- ✅ Легко читать код
- ✅ Быстро находить ошибки
- ✅ Видеть структуру кода
- ✅ Профессиональный вид

### Для пользователей:
- ✅ Редактирование оригинального кода
- ✅ Не нужно расшифровывать обфускацию
- ✅ Удобный редактор
- ✅ Моментальная подсветка

---

## 📊 Сравнение:

### БЕЗ подсветки:
```
local player = game.Players.LocalPlayer
print('Hello ' .. player.Name)
for i,v in pairs(game.Players:GetPlayers()) do
    if v ~= player then
        print(v.Name)
    end
end
```
Всё одним цветом → сложно читать 😕

### С подсветкой:
```lua
local player = game.Players.LocalPlayer
print('Hello ' .. player.Name)
for i,v in pairs(game.Players:GetPlayers()) do
    if v ~= player then
        print(v.Name)
    end
end
```
Каждый элемент своим цветом → легко читать! 😍

---

## 💡 Дополнительные функции:

### 1. Автоматический отступ (Tab)
- Нажатие Tab → 2 пробела
- Стиль: `tab-size: 2`

### 2. Счетчик символов
```
818 characters
```
Обновляется в реальном времени!

### 3. Синхронизация скролла
Подсветка прокручивается вместе с кодом

### 4. Плавная анимация
Подсветка обновляется моментально без задержек

---

## 🎉 Итог:

**Теперь редактирование скриптов - это удовольствие!**

- 🎨 Красивая подсветка синтаксиса Lua
- 📝 Оригинальный код при редактировании
- ⚡ Обновление в реальном времени
- 🖥️ Профессиональный редактор

**Откройте админ панель и попробуйте сами!**
```
http://localhost:3000/admin?pass=admin123&page=scripts
```

Создайте скрипт или отредактируйте существующий - увидите подсветку в действии! ✨

