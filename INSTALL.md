# 📦 Инструкция по установке

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

## Шаг 2: Проверка подключения к MongoDB

База данных уже настроена и готова к использованию:

- **URI**: `mongodb+srv://sahajaret_db_user:62fjyIiAj5kzomUi@cluster0.imymvgf.mongodb.net/?appName=Cluster0`
- **База**: `key_management`
- **Пароль**: Уже встроен в URI

## Шаг 3: Запуск сервера

```bash
npm start
```

Вы увидите:
```
✅ Connected to MongoDB
🚀 Server started on port 3000
📊 MongoDB database: key_management
```

## Шаг 4: Открыть админ панель

Откройте в браузере: `http://localhost:3000/admin`

- **Пароль по умолчанию**: `admin123`

## 🎉 Готово!

Теперь вы можете:
- Создавать ключи через админ панель
- Просматривать статистику
- Управлять ключами
- Все данные сохраняются в MongoDB

## ⚙️ Дополнительные настройки (опционально)

Создайте файл `.env` в корне проекта, если хотите изменить настройки:

```env
PORT=3000
ADMIN_PASSWORD=новый_пароль
MONGODB_URI=ваш_mongodb_uri
```

## 🔧 Устранение проблем

### Ошибка подключения к MongoDB

Убедитесь, что:
1. У вас есть интернет-соединение
2. IP адрес разрешен в MongoDB Atlas (должен быть 0.0.0.0/0)

### Порт уже занят

Если порт 3000 занят, измените его в `.env`:
```env
PORT=3001
```

## 📞 Поддержка

Если возникли проблемы, проверьте:
- Логи в консоли
- Подключение к интернету
- Версию Node.js (рекомендуется 18+)

