# 🎨 CodeMirror - Профессиональный редактор кода

## ✅ Что такое CodeMirror?

**CodeMirror** - это мощный редактор кода, используемый в:
- JSFiddle
- CodePen
- CodeSandbox
- GitHub (для редактирования файлов)
- И многих других сайтах!

---

## 🚀 Что теперь работает:

### 1. **Настоящая подсветка синтаксиса Lua** 🎨
- ✅ Полная поддержка Lua синтаксиса
- ✅ Подсветка ключевых слов
- ✅ Подсветка строк и чисел
- ✅ Подсветка комментариев
- ✅ Подсветка функций

### 2. **Номера строк** 📝
Как в настоящем IDE:
```
1  -- ESP Script
2  local player = game.Players.LocalPlayer
3  local camera = workspace.CurrentCamera
```

### 3. **Автоматический отступ** ⚡
- Нажимаете Enter → автоматический отступ
- Tab = 2 пробела
- Умное форматирование

### 4. **Подсветка активной строки** 💡
Строка где курсор - подсвечивается голубым

### 5. **Поиск парных скобок** 🔍
```lua
function test()  -- ← кликнете на ( 
    print("hi")
end              -- ← и здесь подсветится )
```

### 6. **Темная тема Dracula** 🌙
Красивая профессиональная тема для кода

### 7. **Оригинальный код при редактировании** 📖
Показывается rawContent (не обфусцированный)!

---

## 🎯 Как выглядит:

```
╔═══════════════════════════════════════════════════════╗
║  Lua Code *                                           ║
║  ┌─────────────────────────────────────────────────┐ ║
║  │ 1  -- ESP Script for Roblox                     │ ║
║  │ 2  local player = game.Players.LocalPlayer      │ ║
║  │ 3  local camera = workspace.CurrentCamera       │ ║
║  │ 4                                                │ ║
║  │ 5  -- Configuration                             │ ║
║  │ 6  local settings = {                           │ ║
║  │ 7      enabled = true,                          │ ║
║  │ 8      boxColor = Color3.fromRGB(255, 0, 0)    │ ║
║  │ 9  }                                             │ ║
║  │10                                                │ ║
║  │11  print('ESP Loaded successfully!')            │ ║
║  └─────────────────────────────────────────────────┘ ║
║  440 characters                                      ║
╚═══════════════════════════════════════════════════════╝
```

**С подсветкой синтаксиса:**
- `local`, `function` → фиолетовые
- `player`, `camera` → белые
- `game`, `workspace` → синие
- `'ESP Loaded...'` → зеленые
- `255, 0, 0` → оранжевые
- `-- комментарии` → серые

---

## 📊 Сравнение:

### До (обычный textarea):
```
╔═══════════════════════════════════════════════╗
║ ┌───────────────────────────────────────────┐ ║
║ │local player = game.Players.LocalPlayer    │ ║
║ │print('Hello')                             │ ║
║ └───────────────────────────────────────────┘ ║
╚═══════════════════════════════════════════════╝
```
- ❌ Нет номеров строк
- ❌ Нет подсветки
- ❌ Нет автоотступов
- ❌ Выглядит как блокнот

### После (CodeMirror):
```
╔═══════════════════════════════════════════════╗
║ ┌─┬─────────────────────────────────────────┐ ║
║ │1│local player = game.Players.LocalPlayer  │ ║
║ │2│print('Hello')                           │ ║
║ └─┴─────────────────────────────────────────┘ ║
╚═══════════════════════════════════════════════╝
```
- ✅ Номера строк
- ✅ Подсветка синтаксиса
- ✅ Автоотступы
- ✅ Выглядит как VS Code!

---

## 🎨 Цветовая схема (Dracula):

| Элемент | Цвет | Пример |
|---------|------|--------|
| Фон | Темно-серый | `#282a36` |
| Текст | Белый | `#f8f8f2` |
| Ключевые слова | Розовый | `local`, `function`, `if` |
| Строки | Желтый | `"Hello World"` |
| Числа | Фиолетовый | `123`, `45.67` |
| Комментарии | Серый | `-- comment` |
| Функции | Зеленый | `print`, `game` |

---

## 💡 Горячие клавиши:

| Клавиша | Действие |
|---------|----------|
| **Tab** | Отступ (2 пробела) |
| **Shift+Tab** | Убрать отступ |
| **Ctrl+/** | Закомментировать строку |
| **Ctrl+A** | Выделить всё |
| **Ctrl+Z** | Отменить |
| **Ctrl+Y** | Повторить |
| **Ctrl+F** | Поиск (в будущем) |

---

## 🔧 Технические детали:

### Подключение:
```html
<!-- CSS -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.16/codemirror.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.16/theme/dracula.min.css">

<!-- JavaScript -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.16/codemirror.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.16/mode/lua/lua.min.js"></script>
```

### Инициализация:
```javascript
editor = CodeMirror(document.getElementById('codeEditor'), {
  mode: 'lua',                    // Режим Lua
  theme: 'dracula',               // Темная тема
  lineNumbers: true,              // Номера строк
  lineWrapping: true,             // Перенос длинных строк
  indentUnit: 2,                  // Отступ = 2 пробела
  tabSize: 2,                     // Tab = 2 пробела
  autoCloseBrackets: true,        // Автозакрытие скобок
  matchBrackets: true,            // Подсветка парных скобок
  styleActiveLine: true           // Подсветка активной строки
});
```

### Синхронизация с формой:
```javascript
editor.on('change', function() {
  const content = editor.getValue();
  document.getElementById('scriptContent').value = content;
  document.getElementById('codeSize').textContent = content.length + ' characters';
});
```

---

## 🎯 Использование:

### 1. Создание скрипта:
1. Откройте админку → Scripts
2. Нажмите "New Script"
3. **Увидите CodeMirror редактор!** 🎉
4. Начните печатать - подсветка работает сразу!

### 2. Редактирование:
1. Нажмите "Edit" на любом скрипте
2. **Увидите оригинальный код** (не обфусцированный!)
3. Редактируйте с подсветкой
4. Сохраните

### 3. Пример работы:

**Печатаете:**
```lua
local player = game.Players.LocalPlayer
```

**Видите:**
- `local` - розовый (ключевое слово)
- `player` - белый (переменная)
- `=` - белый (оператор)
- `game` - зеленый (встроенная функция)
- `.Players.LocalPlayer` - белый + зеленый

**Красиво и понятно! 🎨**

---

## 🚀 Преимущества CodeMirror:

### Для разработчика:
- ✅ Профессиональный вид
- ✅ Удобное редактирование
- ✅ Подсветка ошибок (в будущем)
- ✅ Автодополнение (в будущем)

### Для пользователя:
- ✅ Легко читать код
- ✅ Легко найти ошибки
- ✅ Понятная структура
- ✅ Приятная работа

### Технические:
- ✅ Легковесный (~200KB)
- ✅ Быстрый
- ✅ Работает везде
- ✅ Много тем и режимов

---

## 🎨 Доступные темы:

Можно легко сменить тему в коде:

```javascript
theme: 'dracula'    // 🌙 Темная (по умолчанию)
theme: 'monokai'    // 🌃 Monokai
theme: 'material'   // 🎨 Material Design
theme: 'solarized'  // ☀️ Solarized
theme: '3024-night' // 🌌 Ночная
```

---

## 📈 Будущие улучшения:

### Можно добавить:
- [ ] Автодополнение (Ctrl+Space)
- [ ] Поиск и замена (Ctrl+F)
- [ ] Множественный курсор
- [ ] Fold/Unfold блоков кода
- [ ] Линтинг (проверка ошибок)
- [ ] Vim/Emacs режимы
- [ ] Мини-карта кода (как в VS Code)

---

## 🎉 Итог:

**Теперь у вас профессиональный редактор кода!**

- 🎨 Красивая подсветка Lua
- 📝 Номера строк
- ⚡ Умные отступы
- 💡 Подсветка активной строки
- 🔍 Поиск парных скобок
- 🌙 Темная тема Dracula

**Откройте админку и попробуйте!**
```
http://localhost:3000/admin?pass=admin123&page=scripts
```

Создайте или отредактируйте скрипт - увидите разницу сразу! ✨

**Это тот самый "компилятор" о котором вы говорили!** 🚀

