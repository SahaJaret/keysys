# Key Management System

Система управления ключами с MongoDB базой данных.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения (опционально)

Создайте файл `.env` в корне проекта:

```env
# MongoDB Connection
MONGODB_URI=mongodb+srv://sahajaret_db_user:62fjyIiAj5kzomUi@cluster0.imymvgf.mongodb.net/?appName=Cluster0

# Server Configuration
PORT=3000
KEY_BACKEND=http://localhost:3000

# Admin Access
ADMIN_PASSWORD=admin123

# External Services
WORKINK_LINK=https://workink.net/26ij/6z67dpqo
YT_CHANNEL=https://www.youtube.com/@yourchannel

# Discord Webhook (optional)
DISCORD_WEBHOOK_URL=your_webhook_url_here
```

### 3. Запуск сервера

```bash
npm start
```

Сервер будет доступен по адресу: `http://localhost:3000`

## 📊 MongoDB База данных

### Подключение

- **URI**: `mongodb+srv://sahajaret_db_user:62fjyIiAj5kzomUi@cluster0.imymvgf.mongodb.net/?appName=Cluster0`
- **База данных**: `key_management`

### Коллекции

1. **keys** - Хранение ключей доступа
2. **check_logs** - Логи проверки ключей
3. **events** - События системы
4. **traffic** - Статистика трафика

## 🔑 Основные возможности

- ✅ Создание и управление ключами
- ✅ Интеграция с Work.ink
- ✅ YouTube checkpoint
- ✅ Discord уведомления
- ✅ Админ панель
- ✅ Статистика и аналитика
- ✅ Постоянное хранилище (MongoDB)

## 🌐 Endpoints

### Публичные API

- `GET /gate` - Получить URL для генерации ключа
- `GET /get-key` - Страница checkpoint (YouTube)
- `POST /yt-done` - Завершение YouTube checkpoint
- `GET /workink-return` - Обработка возврата от Work.ink
- `GET /check` - Проверка валидности ключа

### Админ панель

- `GET /admin` - Главная страница админ панели
  - Параметр: `pass` (пароль)
  - Страницы: `overview`, `keys`, `services`, `webhooks`, `settings`

## 🔐 Безопасность

По умолчанию пароль админ панели: `admin123`

**⚠️ ВАЖНО**: Измените пароль через переменную окружения `ADMIN_PASSWORD` перед использованием в продакшене!

## 📝 Примеры использования

### Создание ключа (Admin API)

```bash
curl -X POST http://localhost:3000/admin/create-key \
  -d "pass=admin123&hours=24&maxUsage=5"
```

### Проверка ключа

```bash
curl "http://localhost:3000/check?key=YOUR_KEY&userId=12345&username=Player"
```

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
npm run dev
```

## 📦 Зависимости

- **express** - Web framework
- **mongodb** - MongoDB driver

## 📄 Лицензия

ISC

---

💡 **Подсказка**: При первом запуске сервер автоматически создаст необходимые индексы в MongoDB.

