# 💰 Настройка Провайдеров Монетизации

## ✅ Work.ink - ГОТОВ К РАБОТЕ

### Статус: 🟢 Полностью протестирован и работает

### Режим Direct Link (рекомендуется):
1. Получите вашу Work.ink ссылку
2. В админке: Monetization → Work.ink → Configure
3. Вставьте ссылку
4. Сохраните

### Режим API Key (опционально):
1. Получите API ключ из Work.ink Dashboard
2. Выберите режим "🔑 API Key"
3. Вставьте ключ
4. Сохраните

**Callback URL для Work.ink:**
```
http://your-domain.com/monetization-callback/workink
```

**Что работает:**
- ✅ Валидация токенов через публичный API
- ✅ Автоматическое создание ключей
- ✅ Статистика
- ✅ Обработка callback

---

## ⚠️ Lootlabs - ТРЕБУЕТ НАСТРОЙКИ

### Статус: 🟡 Базовая структура готова, нужна валидация

### Что нужно сделать:

#### 1. Проверьте документацию Lootlabs
Зайдите в: https://loot-link.com/ → Developer/API

#### 2. Узнайте:
- **Какие параметры приходят на callback?**
  - Например: `?token=xxx`, `?user_id=xxx`, `?completed=true`
  
- **Есть ли API endpoint для валидации?**
  - Например: `https://loot-link.com/api/validate`
  
- **Формат авторизации API ключа:**
  - Bearer token?
  - API key в header?
  - В body запроса?

#### 3. Обновите код:
Откройте `server (2).js` и найдите секцию:
```javascript
} else if (providerId === "lootlabs") {
  // ⚠️ ТРЕБУЕТ НАСТРОЙКИ
```

Добавьте валидацию согласно документации Lootlabs.

**Пример (если есть API):**
```javascript
if (provider.config.useApiKey) {
  const response = await fetch('https://loot-link.com/api/validate', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${provider.config.apiKey}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ token })
  });
  
  const data = await response.json();
  if (!data.valid) {
    return res.status(403).send("Invalid token");
  }
}
```

**Callback URL для Lootlabs:**
```
http://your-domain.com/monetization-callback/lootlabs
```

---

## ⚠️ Linkvertise - ТРЕБУЕТ НАСТРОЙКИ

### Статус: 🟡 Базовая структура готова, нужна валидация

### Что нужно сделать:

#### 1. Проверьте документацию Linkvertise
Зайдите в: https://linkvertise.com/pages/api

#### 2. Узнайте:
- **Формат callback параметров:**
  - `?token=xxx`
  - `?target=xxx` (ваш callback URL)
  - `?timestamp=xxx`

- **Требуется ли валидация?**
  - Или Linkvertise автоматически редиректит только при успехе?

- **API endpoint (если используете API режим):**
  - URL для валидации
  - Метод авторизации

#### 3. Обновите код:
Найдите секцию:
```javascript
} else if (providerId === "linkvertise") {
  // ⚠️ ТРЕБУЕТ НАСТРОЙКИ
```

**Пример валидации (если нужна):**
```javascript
if (provider.config.useApiKey) {
  const response = await fetch('https://api.linkvertise.com/api/v1/validate', {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${provider.config.apiKey}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({ 
      token,
      user_id: provider.config.userId 
    })
  });
  
  const data = await response.json();
  if (!data.success) {
    return res.status(403).send("Invalid token");
  }
}
```

**Callback URL для Linkvertise:**
```
http://your-domain.com/monetization-callback/linkvertise
```

---

## 🧪 Тестирование

### 1. Проверьте callback вручную:
```bash
# Для Work.ink (работает):
curl "http://localhost:3000/monetization-callback/workink?token=test123"

# Для Lootlabs (проверьте что приходит):
curl "http://localhost:3000/monetization-callback/lootlabs?token=test123"

# Для Linkvertise (проверьте что приходит):
curl "http://localhost:3000/monetization-callback/linkvertise?token=test123"
```

### 2. Смотрите логи сервера:
```bash
# В консоли сервера вы увидите:
⚠️ Lootlabs: Token validation not implemented. Check their API docs!
⚠️ Linkvertise: Token validation not implemented. Check their API docs!
```

### 3. Тестируйте с реальными токенами после настройки

---

## 📋 Чеклист настройки

### Work.ink ✅
- [x] Получить Work.ink ссылку
- [x] Настроить в админке
- [x] Добавить callback URL в Work.ink
- [x] Протестировать

### Lootlabs ⚠️
- [ ] Изучить документацию Lootlabs
- [ ] Узнать формат callback параметров
- [ ] Получить API ключ (если нужен)
- [ ] Обновить код валидации
- [ ] Настроить callback URL в Lootlabs
- [ ] Протестировать

### Linkvertise ⚠️
- [ ] Изучить документацию Linkvertise
- [ ] Узнать формат callback параметров
- [ ] Получить API ключ и User ID (если нужен)
- [ ] Обновить код валидации
- [ ] Настроить callback URL в Linkvertise
- [ ] Протестировать

---

## 💡 Рекомендации

1. **Начните с Work.ink** - он работает "из коробки"
2. **Тестируйте на localhost** перед продакшеном
3. **Используйте ngrok** для тестирования callbacks локально:
   ```bash
   ngrok http 3000
   # Используйте ngrok URL для callback: https://xxx.ngrok.io/monetization-callback/...
   ```
4. **Логируйте все параметры** при первых тестах
5. **Проверяйте консоль сервера** на предупреждения

---

## 🆘 Нужна помощь?

Если у вас есть доступ к документации Lootlabs или Linkvertise, пришлите мне:
1. Формат callback параметров
2. Endpoint для валидации (если есть)
3. Формат авторизации

Я помогу настроить интеграцию правильно!

